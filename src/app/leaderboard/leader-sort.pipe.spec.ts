import { LeaderSortPipe } from './leader-sort.pipe';

describe('LeaderSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaderSortPipe();
    expect(pipe).toBeTruthy();
  });
});
